apiVersion: 1

providers:
  - name: "EHub Dashboards"
    orgId: 1
    folder: "Monitoring"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards/json
