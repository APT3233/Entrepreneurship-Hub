{
  "name": "entrepreneurship-hub",
  "private": true,
  "scripts": {
    "predev": "docker compose stop nginx ehub-client ehub-server 2>/dev/null || true",
    "dev": "concurrently -k -n server,client -c blue,green \"npm run dev --prefix ehub-server\" \"npm run dev --prefix ehub-client\"",
    "dev:server": "npm run dev --prefix ehub-server",
    "dev:client": "npm run dev --prefix ehub-client",
    "dev:infra": "docker compose up -d mysql redis minio ollama ehub-mail-worker ehub-ai-worker",
    "install:all": "npm install --prefix ehub-server && npm install --prefix ehub-client",
    "postinstall": "npm run install:all"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
